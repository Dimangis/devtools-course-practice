#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_run_program
    COMMAND ${APPLICATION}
)
set_tests_properties (${prefix}_run_program PROPERTIES
    PASS_REGULAR_EXPRESSION "Invalid argument amount, try --help or -h commands"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_get_help
    COMMAND ${APPLICATION} --help
)
set_tests_properties (${prefix}_get_help PROPERTIES
    PASS_REGULAR_EXPRESSION "This program considers the integral"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_test1
    COMMAND ${APPLICATION} -t1 QWERTYUIOASDFGHJKLZXCVBNM
)
set_tests_properties (${prefix}_test1 PROPERTIES
    PASS_REGULAR_EXPRESSION "TZHUWBXLRDVGIJKMNOCAFYEQP\nsuccess"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_test2
    COMMAND ${APPLICATION} -t2 TZHUWBXLRDVGIJKMNOCAFYEQP
)
set_tests_properties (${prefix}_test2 PROPERTIES
    PASS_REGULAR_EXPRESSION "QWERTYUIOASDFGHJKLZXCVBNM\nsuccess"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_test3
    COMMAND ${APPLICATION} -t3 TEST
)
set_tests_properties (${prefix}_test3 PROPERTIES
    PASS_REGULAR_EXPRESSION "WHVW\nsuccess"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_test4
    COMMAND ${APPLICATION} -t4 HELLO WORLD
)
set_tests_properties (${prefix}_test4 PROPERTIES
    PASS_REGULAR_EXPRESSION "KHOOR ZRUOG\nsuccess"
    LABELS "${MODULE}")


